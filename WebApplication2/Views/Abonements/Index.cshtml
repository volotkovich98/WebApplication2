@model IEnumerable<WebApplication2.Abonement>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NumberOfVisits)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Price)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumberOfVisits)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AbonementId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AbonementId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AbonementId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
